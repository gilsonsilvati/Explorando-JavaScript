// Cria array
var emails = [];

console.log(typeof emails);
console.log(emails.length);

// Adiciona elemento no final
emails.push('joao@email.com');
console.log(emails);

// Adiciona elemento no final
emails.push('maria@email.com');
console.log(emails);

// Adiciona elemento no final
emails.push('jose@email.com');
console.log(emails);

// Remove elemento do final
emails.pop();
console.log(emails);

// Adiciona elemento no inicio
emails.unshift('gilsonsilvati@gmail.com');
console.log(emails);

// Remove elemento no inicio
emails.shift();
console.log(emails);
